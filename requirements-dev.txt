# =============================================================================
# DYD AI SYLHET — development, test and security tooling
# =============================================================================
# NEVER installed on the live server. `deploy.sh` installs requirements.txt only.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# These implement the §19.1 test pyramid and the §8.2 pre-commit gates:
#   npm run css:build && npm run css:check · ruff · mypy · pytest --cov=app
# -----------------------------------------------------------------------------

-r requirements.txt

# ── Unit / integration / data tests (plan.md §19.1) ────────────────────────
pytest==9.1.1               # 70% coverage gate on app/services
pytest-cov==7.1.0           # `pytest --cov=app`

# ── E2E, visual and a11y tests (plan.md §19.1, §19.2) ──────────────────────
pytest-playwright==0.9.0    # the 6 journeys + 5 pages at 360/768/1440
playwright==1.63.0          # browser engines are a separate step, see below

# ── Lint and type gates (plan.md §8.2) ─────────────────────────────────────
ruff==0.16.8                # lint + import order
mypy==2.3.1                 # static types

# ── Security gates (plan.md §19.1) ─────────────────────────────────────────
bandit==1.9.4               # SAST; must report zero high
pip-audit==2.10.1           # dependency CVE scan of the pinned set

# ── Load testing (plan.md §19.1) ───────────────────────────────────────────
locust==2.46.6              # 300 concurrent reader sessions, p95 < 1.5 s

# -----------------------------------------------------------------------------
# One-time browser download for Playwright (not needed to run the app):
#   .venv\Scripts\python.exe -m playwright install chromium
# Deferred until the M8 E2E suite exists — it is a ~150 MB download.
# -----------------------------------------------------------------------------
